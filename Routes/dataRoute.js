const express = require('express');
const router = express.Router();


router.get('/:dbname/:collectionname/putData');
router.get('/:dbname/:collectionname/show');


router.post('/:dbname/:collectionname/putData');
