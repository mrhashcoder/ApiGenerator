<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Collection</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
</head>
<body>
    <h1>Welcome to Collection</h1>
    <h2>Collection Schema : </h2>
    <h2><a href="/db">All Database</a></h2>
    <h2><a href="/<%= dbname %> "><%= dbname %> </a></h2>
    <div style="border: solid black 2px ; margin:10px">
        <h3>DataBase : <%= dbname %> </h3>
        <h3>Name : <%= collectionname %> </h3>
        <h3>Number of Enteries : <%= Ents.length %> </h3>
        <h3>Elements of Collection : 
            <% for( let index = 0; index < Ents.length; index++ ) { %>
                <span><%= Ents[index] %>,  </span>
            <% } %>
        </h3>

        <div style="border: solid black 2px ; margin:10px">
            <h2>Uploaded Data to this Collection : </h2>
            <hr>
            <% var index = 0 %>
            <% dataCol.forEach(data => { %>                 
                <%Ents.forEach(ent => { %>
                    <h3>
                        <span><%= ent %> : </span>
                        <span><%= data[ent] %> </span>
                        <span>API here : <a href="/api/<%= dbname %>/<%= collectionname %>/<%= index %>">link</a></span>
                    </h3>
                <% }) %>
                <% index += 1 %> 
                <hr>
            <% }) %>    
        </div>
        <h3>API of this Collection : <a href="/api/<%= dbname %>/<%= collectionname %>">link</a></h3>
    </div>
</body>
</html>